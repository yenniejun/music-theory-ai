# Default OMR engine (oemer | audiveris | claude)
OMR_PROVIDER=oemer

# Only used when OMR_PROVIDER=claude
ANTHROPIC_API_KEY=sk-ant-...

# Optional binary overrides
# OEMER_BIN=oemer
# AUDIVERIS_BIN=audiveris
